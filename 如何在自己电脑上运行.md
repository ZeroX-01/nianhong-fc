# 《那年的红白机》——在自己电脑上怎么跑

这份说明是给"不太熟命令行"的人看的。按顺序读，挑一个方案就行。

---

## 方案 A（最省事，什么都不用装）

直接用浏览器打开在线版（地址在仓库首页右侧的 About 里，也在 README 开头）。

推荐 Chrome / Edge / Safari。打开后先点一下屏幕上的 **"点击开机"**，游戏才会出声（见下面"为什么要点一下才有声音"）。

如果只是想玩、想给别人演示，到这里就够了，下面都不用看。

---

## 方案 B（本地跑，推荐给要改代码的场景）——用 Python 起一个小服务器

这个项目是纯静态网页，**不需要 npm install，不需要编译**，只要有一个"本地 http 服务"把文件端出来就行。

### macOS（系统自带 Python3，不用装东西）

1. 把 `nianhong-fc` 源码包 解压，得到一个 `nianhong-fc` 文件夹。
2. 打开「终端」（聚焦搜索里输入 Terminal 回车）。
3. 输入 `cd ` （注意 cd 后面有个空格），然后把解压出来的 `nianhong-fc` 文件夹**直接拖进终端窗口**，回车。
   - 效果等同于：`cd ~/Downloads/nianhong-fc`
4. 执行：

   ```bash
   python3 -m http.server 8100
   ```

5. 看到 `Serving HTTP on ... port 8100` 就成功了。打开浏览器访问：

   **http://localhost:8100/index.html**

6. 玩完了回到终端按 `Control + C` 停掉服务。

### Windows

1. 解压 zip，得到 `nianhong-fc` 文件夹。
2. 在这个文件夹的地址栏里输入 `cmd` 回车（会在当前目录打开命令提示符）；或者打开 PowerShell 后 `cd` 到该目录。
3. 执行（Windows 上命令名通常是 `python`，不是 `python3`）：

   ```bat
   python -m http.server 8100
   ```

4. 浏览器打开 **http://localhost:8100/index.html**
5. 如果提示找不到 python，说明没装：去 python.org 下载安装，安装时勾选 "Add Python to PATH"；或者直接用方案 A / 方案 D。

> 8100 这个端口号可以随便换，比如 `8123`，只要浏览器地址里的端口跟着改。若提示端口被占用，换一个数字即可。

---

## 方案 C（装了 Node 的话）

在解压后的 `nianhong-fc` 目录里执行任意一条：

```bash
npx serve -l 8100
```

或

```bash
npx http-server -p 8100
```

第一次执行会自动下载小工具（需要联网），之后按它打印的地址打开（一般是 http://localhost:8100 ）。

---

## 方案 D（用 VS Code，鼠标点点就行）

1. 用 VS Code 打开解压出来的 `nianhong-fc` 文件夹。
2. 左侧扩展面板搜索 **Live Server**（作者 Ritwick Dey），安装。
3. 在文件列表里 **右键 `index.html` → Open with Live Server**。
4. 浏览器会自动打开，地址形如 `http://127.0.0.1:5500/index.html`。

---

## ⚠️ 最重要的一条：不要双击 index.html

**千万不要直接双击 `index.html`**，那样浏览器地址栏是 `file:///...` 开头。
在 `file://` 下浏览器的安全策略会拦掉音频、字体和资源加载，结果就是**白屏或者没声音**。

必须是 `http://localhost:...` 或 `http://127.0.0.1:...` 开头才正常。上面 B/C/D 四个方案做的就是这件事。

---

## 为什么要先点一下"点击开机"才有声音

现代浏览器规定：页面必须先收到一次用户点击，才允许播放声音（叫"音频解锁"）。
所以首屏那句"点击开机"不是装饰，**必须点一下**，之后 BGM 和音效才会出来。

---

## 操作键位表

> **先说清一个容易搞混的点**：界面上写的 **A / B 指的是红白机手柄上的 A 键、B 键**，
> 在键盘上对应的是 **X 键（=A）** 和 **Z 键（=B）**，**不是**字母 A 键和字母 B 键。

### 1P（一号手柄）

| 手柄按钮 | 键盘按键 | 常见作用 |
|---|---|---|
| 方向 上/下/左/右 | ↑ ↓ ← → 方向键；**在小游戏里 W / A / S / D 也一样** | 移动、菜单上下选择 |
| **A** | **X** / **K** / **空格**（小游戏以外还有 **A**） | 跳跃、确认 |
| **B** | **Z** / **J** | 攻击/射击/加速、取消返回 |
| START | **Enter** | 开始、暂停 |
| SELECT | **Shift** | 选择、切换 |

一个人玩的时候，进了电视里的小游戏，`W A S D` 就是方向键 —— 很多人一坐下就这么按。这时候「确认 / 开火」请按 `X`（`Z` 是 B 键）；退出小游戏、在客厅和菜单里，`A` 又变回确认。每张卡真的能动的那一刻，电视画面里会弹一张「怎么玩」，写的就是当时真正管用的键。

### 2P（二号手柄）

| 手柄按钮 | 键盘按键 |
|---|---|
| 方向 上/下/左/右 | **W / S / A / D** |
| A | **H** |
| B | **G** |

（2P 的方向用的是 WASD，这里的 A 和 D 才是字母键，用于左右移动。两个人一起打的时候，WASD 只归 2P，1P 请用方向键。）

---

## 常见问题（FAQ）

### 1. 没有声音？
- 先点一下首屏的"点击开机"。
- 检查地址栏是不是 `http://` 开头，`file://` 一定没声音。
- 检查系统音量、浏览器标签页有没有被静音（标签上有小喇叭叉号就右键取消静音）。
- 游戏里有音量/静音设置项，确认没关掉。
- Safari 偶尔更严格，换 Chrome 试试。

### 2. 页面白屏 / 只有黑框？
1. 确认是 `http://localhost:8100/index.html`，不是 `file://`。
2. 按 **F12**（macOS 是 `Command + Option + I`）打开浏览器控制台，看 Console 和 Network 里的红色报错：
   - 大量 404 → 说明解压不完整，或者你的服务不是在 `nianhong-fc` 这一层目录启动的（`index.html` 必须和你启动服务的目录在同一层）。
   - 提到 `phaser` 未定义 → `vendor/phaser.min.js` 没带上，重新解压完整的 zip。
3. 强制刷新一次：`Ctrl + Shift + R` / `Command + Shift + R`。

### 3. 我的存档在哪？会不会丢？
存档写在浏览器的 **localStorage** 里，跟"这个浏览器 + 这个网址"绑定。所以：
- 换浏览器（Chrome → Safari）会看不到原来的存档；
- 在线版和本地版（localhost）的存档是**两份**，互不相通；
- 清理浏览器数据 / 用无痕窗口，存档会丢。
- 想留着就别清浏览器数据。

### 4. 端口被占用了怎么办？
把 8100 换成别的数字（8123、9000 都行），浏览器地址里的端口跟着改。

### 5. 怎么跑自动化测试？（可选，不影响玩）
测试脚本在 `tools/gametest/` 下，用 Node + Playwright 驱动真实浏览器跑。需要：

```bash
# 1) 先在一个终端窗口把本地服务跑起来（方案 B）
python3 -m http.server 8100

# 2) 另开一个终端窗口，在 nianhong-fc 目录里：
npm i playwright
npx playwright install chromium

# 3) 跑其中一个测试
node tools/gametest/systems_test.js
```

`tools/gametest/` 里还有 `flow_test.js`、`settings_test.js`、`tank_test.js`、`fight_test.js` 等，用法同上。
这些脚本可能会在目录里生成截图 png，属于正常产物，可以随手删掉。
**这一步纯属可选**，只是方便 review 逻辑，玩游戏完全不需要。

---

## 包里都有什么

| 路径 | 说明 |
|---|---|
| `index.html` | 入口页面，服务起来后访问它 |
| `src/` | 全部游戏源码（场景、系统、四个小游戏） |
| `vendor/phaser.min.js` | Phaser 3.60 引擎本体（已随包提供，项目**不联网**） |
| `assets/img`、`assets/audio`、`assets/font` | 图片、音频（mp3 + ogg 双份）、字体 |
| `docs/` | 技术文档，`docs/prd_src/` 是核心系统/场景/玩法/测试用例的事实文档 |
| `tools/gametest/` | 自动化测试脚本 |
| `tools/build_dist.sh` | 构建发布产物的脚本（本地玩不需要执行） |
| `README.md` | 项目总览 |
